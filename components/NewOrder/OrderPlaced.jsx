import React from "react";
import { Text } from "native-base";

const OrderPlaced = ({ navigation }) => {
	const { navigate } = navigation; 
	return (
		<Text>Order placed jsx</Text>
	);
};

export default OrderPlaced;