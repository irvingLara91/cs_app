import { StyleSheet } from "react-native";

module.exports = StyleSheet.create({
	flexSpaceBetween: {
		display: "flex", 
		width: "100%", 
		flexDirection: "row", 
		justifyContent: "space-between"
	}
});